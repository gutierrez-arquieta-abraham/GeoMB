<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/bottom_nav"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Barra inferior propia (LinearLayout) para no toparse con el límite de 5 -->
    <LinearLayout
        android:id="@+id/bottom_nav"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="?attr/colorSurface"
        android:orientation="horizontal"
        android:elevation="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout android:id="@+id/nav_mapa" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_map" android:contentDescription="@string/nav_mapa" />
            <TextView style="@style/NavItemText" android:text="@string/nav_mapa" />
        </LinearLayout>

        <LinearLayout android:id="@+id/nav_buscar" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_search" android:contentDescription="@string/nav_buscar" />
            <TextView style="@style/NavItemText" android:text="@string/nav_buscar" />
        </LinearLayout>

        <LinearLayout android:id="@+id/nav_lineas" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_bus" android:contentDescription="@string/nav_lineas" />
            <TextView style="@style/NavItemText" android:text="@string/nav_lineas" />
        </LinearLayout>

        <LinearLayout android:id="@+id/nav_ruta" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_route" android:contentDescription="@string/nav_ruta" />
            <TextView style="@style/NavItemText" android:text="@string/nav_ruta" />
        </LinearLayout>

        <LinearLayout android:id="@+id/nav_llegadas" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_bell" android:contentDescription="@string/nav_llegadas" />
            <TextView style="@style/NavItemText" android:text="@string/nav_llegadas" />
        </LinearLayout>

        <LinearLayout android:id="@+id/nav_reporte" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_report" android:contentDescription="@string/nav_reporte" />
            <TextView style="@style/NavItemText" android:text="@string/nav_reporte" />
        </LinearLayout>

        <LinearLayout android:id="@+id/nav_acerca" style="@style/NavItem">
            <ImageView android:layout_width="24dp" android:layout_height="24dp" android:src="@drawable/ic_info" android:contentDescription="@string/nav_acerca" />
            <TextView style="@style/NavItemText" android:text="@string/nav_acerca" />
        </LinearLayout>

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
